#include <stdio.h>

int iterationMethod(int n) {
    int result = 0;
    for (int i = 1; i <= n; i++) {
        result += 2 * i + 1;
    }
    return result;
}

int main() {
    int n = 8;
    printf("Result using Iteration Method: %d\n", iterationMethod(n));
    return 0;
}
